services:
  - type: web
    name: client-management-system
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3000
    disk:
      name: sqlite-data
      mountPath: /app/data
      sizeGB: 1
